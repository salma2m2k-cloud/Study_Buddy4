# No custom ProGuard rules needed — release builds ship with
# minifyEnabled false by default (see android/app/build.gradle).
# If you enable minification later, keep flutter_local_notifications'
# generated code:
# -keep class com.dexterous.** { *; }
